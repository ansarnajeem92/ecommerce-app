# Ecommerce App

A fullstack ecommerce application built with React, Express, PostgreSQL, and Docker.

## Run locally

```bash
docker compose up --build
```

## Structure

- frontend/ — React + Tailwind
- backend/ — Express + PostgreSQL
- docker-compose.yml — Multi-container setup
